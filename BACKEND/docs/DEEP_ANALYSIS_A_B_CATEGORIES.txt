╔═══════════════════════════════════════════════════════════════════════════╗
║           NEXURA SCANNER — CHUQUR ANALIZ VA KATEQORYALASH                 ║
║     Muammolar: SERVER DEPLOYMENT vs CODE/TECHNOLOGY MUAMMOLARI             ║
║                          2026-06-08                                        ║
╚═══════════════════════════════════════════════════════════════════════════╝

🎯 IJTIMA'I: Hammasini 2 kataqoryaga ajratdik
═══════════════════════════════════════════════════════════════════════════

A QISMI: SERVER/DEPLOYMENT MUAMMOLARI (Production Serverga Deploy O'tashdan Keyin Hal Qilinadigan)
B QISMI: CODE & TECHNOLOGY MUAMMOLARI (Dastur Kodini Tuzatlash Kerak)

═══════════════════════════════════════════════════════════════════════════
═════════════════════════════════════════════════════════════════════════════

🔴 A QISMI: SERVER/DEPLOYMENT MUAMMOLARI
═══════════════════════════════════════════════════════════════════════════

Bu muammolarni hal qilish uchun: Production server, config, installed tools,
environment variables kerak. Kod o'ziga ko'ra FIXED.

┌─ A1: EXTERNAL TOOLS DEPENDENCY ─────────────────────────────────────────┐
│ Problem:                                                                  │
│  - nmap, nuclei, nikto, sqlmap, gobuster, amass, whatweb INSTALLED      │
│    bo'lishi kerak server'da                                              │
│  - Agar tool yo'q bo'lsa, scan fail qiladi (gracefully degrade bo'ladi   │
│    lekin full scanning ishlamasligi mumkin)                              │
│  - Windows'da PATH'ga nmap special handling kerak                         │
│  - Her tool'ning CLI interface farqi (args va output format)              │
│                                                                           │
│ Where: nexura/runner.py (line 71-76), nexura/web/app.py (line 308-322) │
│ Risk Level: HIGH — Scanner failed bo'ladi agar tools yo'q               │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Server'da barcha tools o'natlash (setup script bo'ladi)              │
│  2. NEXURA_NMAP_PATH env var bilan custom paths                          │
│  3. Graceful degradation — missing tools uchun skip dengan error log    │
│  4. Health check endpoint — /api/status da tool availability             │
│  5. Docker image bilan built-in tools                                    │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A2: MODEL DOWNLOAD & CACHE ────────────────────────────────────────────┐
│ Problem:                                                                  │
│  - GGUF model (Qwen2.5-7B) 4.7 GB - download va cache kerak              │
│  - Agar model yo'q, AI features disabled bo'ladi                         │
│  - Automatic download implemented (scripts/download_model.py)            │
│  - Lekin server startup'da blocking operation bo'lishi mumkin             │
│                                                                           │
│ Where: nexura/config.py (line 17), nexura/__main__.py                   │
│ Risk Level: MEDIUM — Server start slow, AI engine disabled              │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Async model loading (startup event'da background task)               │
│  2. Model memoization/caching (thread-safe)                              │
│  3. Fallback mode — model yo'q bo'lsa API basic mode bilan               │
│  4. Pre-fetch model Docker build'da                                      │
│  5. Model management UI — upload, delete, select models                  │
│  6. Storage check — disk space validation                                │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A3: DATABASE INITIALIZATION & MIGRATION ──────────────────────────────┐
│ Problem:                                                                  │
│  - SQLite database automatic creation bo'ladi lekin:                      │
│  - Schema versioning yo'q (migration tool yo'q)                          │
│  - Agar schema o'zgargansa, old DB'lar corrupt bo'lishi mumkin           │
│  - Concurrent access'da WAL mode'da lock issues bo'lishi mumkin          │
│  - Backup/restore mechanism yo'q                                         │
│                                                                           │
│ Where: nexura/history_db.py (line 20-75), nexura/config.py              │
│ Risk Level: MEDIUM — Data loss yoki corruption risk                     │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Database migration system (version-based schema updates)             │
│  2. Backup mechanizmi (daily rotate backup)                              │
│  3. Connection pool management (max connections limit)                   │
│  4. DB health check endpoint (/api/db-health)                            │
│  5. WAL mode'da shared cache config                                      │
│  6. VACUUM va ANALYZE cronjob                                            │
│  7. Export/Import JSON reports backup'ining o'rniga                      │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A4: ENVIRONMENT VARIABLES & CONFIGURATION MANAGEMENT ──────────────────┐
│ Problem:                                                                  │
│  - config.json va env vars aralash bo'lib qisiladi                        │
│  - Secret management yo'q (API keys, DB passwords)                       │
│  - NEXURA_API_KEY env var default empty bo'lsa, API malumotlar           │
│    public bo'lib qoladi                                                   │
│  - Different env (dev, staging, prod) uchun config yo'q                  │
│                                                                           │
│ Where: nexura/config.py, nexura/web/app.py (line 55-57)                 │
│ Risk Level: CRITICAL — Security breach risk                             │
│                                                                           │
│ SOLUTION:                                                                │
│  1. .env file management (python-dotenv)                                 │
│  2. Secrets rotation system (periodic regenerate)                        │
│  3. Multi-env config (dev.env, prod.env, test.env)                       │
│  4. API key generation va validation                                     │
│  5. Env variable validation at startup                                   │
│  6. Config schema validation (pydantic BaseSettings)                     │
│  7. Masked logging (###### API key'larini conceal)                       │
│  8. Built-in config generator script                                     │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A5: LOGGING & MONITORING ──────────────────────────────────────────────┐
│ Problem:                                                                  │
│  - Logging inconsistent (inglizcha/uzbekcha aralash)                     │
│  - Log rotation jo'q (nexura.log grow unbounded)                         │
│  - No structured logging (JSON format yo'q)                              │
│  - Monitoring/alerting system yo'q                                       │
│  - Error tracking (Sentry/DataDog) integration yo'q                      │
│                                                                           │
│ Where: Throughout codebase, nexura.log file                             │
│ Risk Level: MEDIUM — Debugging difficult, no audit trail                │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Structured logging (Python logging + JSON formatter)                 │
│  2. Log rotation (RotatingFileHandler 10MB, keep 5 files)                │
│  3. Log levels environment-based (DEBUG in dev, ERROR in prod)           │
│  4. Metrics collection (Prometheus compatible endpoints)                 │
│  5. Health check logging separate file                                   │
│  6. Sentry integration optional                                          │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A6: HTTPS/SSL CERTIFICATE MANAGEMENT ─────────────────────────────────┐
│ Problem:                                                                  │
│  - Default HTTP server (uvicorn), HTTPS'siz                              │
│  - SSL_VERIFY config lekin root CA'lar?                                  │
│  - Self-signed certificate'lar demo'da saklanadi                         │
│  - Certificate rotation mechanism yo'q                                   │
│  - Let's Encrypt integration yo'q                                        │
│                                                                           │
│ Where: nexura/web/app.py (line 57), nexura/config.py (line 29)          │
│ Risk Level: CRITICAL — Man-in-the-middle attacks                        │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Nginx reverse proxy (SSL termination)                                │
│  2. Let's Encrypt certificate automation (certbot)                       │
│  3. Self-signed cert generation script (dev mode)                        │
│  4. Certificate validation at startup                                    │
│  5. HSTS headers                                                         │
│  6. TLS 1.3 minimum                                                      │
│  7. Certificate monitoring/renewal alerts                                │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A7: SYSTEMD SERVICE / WINDOWS SERVICE INTEGRATION ─────────────────────┐
│ Problem:                                                                  │
│  - No system service (systemd, Windows Service)                          │
│  - Manual start/stop scripts (.bat files)                                │
│  - Auto-restart on crash yo'q                                           │
│  - Startup ordering (DB -> Server) kontrol qilinmagani                   │
│  - Log rotation / cleanup cronjob yo'q                                   │
│                                                                           │
│ Where: start.bat, stop.bat (basic scripts)                               │
│ Risk Level: MEDIUM — Downtime, resource exhaustion                      │
│                                                                           │
│ SOLUTION:                                                                │
│  1. systemd service file (Linux)                                         │
│  2. Windows Service installer (nssm or NSIS)                             │
│  3. Health check script (restart if down)                                │
│  4. Resource limits (memory, CPU)                                        │
│  5. Log cleanup cronjob                                                  │
│  6. Supervisor configuration (failover)                                  │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A8: CROSS-ORIGIN RESOURCE SHARING (CORS) HARDENING ─────────────────┐
│ Problem:                                                                  │
│  - CORS whitelist hard-coded (line 39-43)                                │
│  - Development URLs qatiq (localhost:5173, localhost:8080)               │
│  - Frontend yo'q bo'lsa redundant                                        │
│  - Wildcard CORS '*' production'da risk                                  │
│                                                                           │
│ Where: nexura/web/app.py (line 39-51)                                   │
│ Risk Level: MEDIUM — XSS/CSRF attacks possible                          │
│                                                                           │
│ SOLUTION:                                                                │
│  1. CORS environment-based configuration                                 │
│  2. Prod'da strict origin check                                          │
│  3. Preflight caching optimization                                       │
│  4. Credentials mode explicit                                            │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A9: RATE LIMITING & DDOS PROTECTION ──────────────────────────────────┐
│ Problem:                                                                  │
│  - No rate limiting (user bolsa 1000 req/sec jo'natsa crash bo'ladi)    │
│  - No request queueing                                                   │
│  - No timeout enforcement server-level                                   │
│                                                                           │
│ Where: nexura/web/app.py (entire API)                                   │
│ Risk Level: HIGH — DoS vulnerability                                    │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Redis-based rate limiting (slowapi)                                  │
│  2. Per-IP rate limits                                                   │
│  3. Per-endpoint rate limits                                             │
│  4. Request queue with priority                                          │
└────────────────────────────────────────────────────────────────────────────┘

┌─ A10: RESOURCE CLEANUP & DISK SPACE MANAGEMENT ─────────────────────┐
│ Problem:                                                                  │
│  - Reports folder growing unbounded (reports/*.json, reports/*.html)    │
│  - Disk space monitoring yo'q                                            │
│  - Old reports manual cleanup kerak                                      │
│  - Temporary files (partial scans, temp DB) cleanup yo'q                 │
│                                                                           │
│ Where: nexura/report/generator.py, reports/ folder                      │
│ Risk Level: MEDIUM — Filesystem full, service crash                     │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Auto-cleanup cronjob (reports > 30 days old)                         │
│  2. Disk space alert threshold (90% warning, 95% critical)               │
│  3. Report retention policy (keep latest N reports)                      │
│  4. Archival system (old reports -> tar.gz)                              │
│  5. Monitoring endpoint (/api/disk-usage)                                │
└────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
═════════════════════════════════════════════════════════════════════════════

🔵 B QISMI: CODE & TECHNOLOGY MUAMMOLARI
═══════════════════════════════════════════════════════════════════════════

Bu muammolarni hal qilish uchun: Kod tuzatlash, refactoring, bug fixes,
library updates kerak. Server kerak emas, dev environment'da solve bo'ladi.

┌─ B1: PYDANTIC & TYPE SAFETY IMPROVEMENTS ───────────────────────────────┐
│ Problem:                                                                  │
│  - ✅ FIXED: Mutable defaults (list[], dict{})                           │
│  - ❌ TBD: Validator'lar missing (negative port, invalid IP)              │
│  - ❌ TBD: Custom error messages sparse                                  │
│  - ❌ TBD: Serialization edge cases (datetime format inconsistent)        │
│                                                                           │
│ Where: nexura/models/schemas.py                                         │
│ Impact: Data integrity, API contract clarity                            │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Field validators (port range 1-65535, valid IP/hostname)            │
│  2. Config validation schema                                             │
│  3. Comprehensive error messages                                         │
│  4. datetime serialization format standardize (ISO 8601)                 │
│  5. API response envelope (success/error fields)                         │
│  6. Custom JSON encoders                                                 │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B2: ERROR HANDLING & RESILIENCE ──────────────────────────────────────┐
│ Problem:                                                                  │
│  - ✅ FIXED: ai_engine.py response parsing                               │
│  - ❌ TBD: nmap parser line 31 — "ports" element missing exception       │
│  - ❌ TBD: Target validation edge cases (IPv6, port:host)                 │
│  - ❌ TBD: Subprocess timeout handling incomplete                        │
│  - ❌ TBD: CVE lookup network errors no retry logic                      │
│  - ❌ TBD: Race conditions in concurrent scans                            │
│                                                                           │
│ Where: nexura/parsers/nmap.py, nexura/runner.py, nexura/tool_selector.py
│ Impact: Stability, service availability                                 │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Exception handling comprehensive (nmap XML parsing)                  │
│  2. Exponential backoff retry strategy (CVE lookup)                      │
│  3. Circuit breaker pattern (external API failures)                      │
│  4. Timeout management granular (tool-specific timeouts)                 │
│  5. Graceful degradation strategy                                        │
│  6. Error instrumentation (error metrics)                                │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B3: PERFORMANCE & CONCURRENCY IMPROVEMENTS ────────────────────────────┐
│ Problem:                                                                  │
│  - ThreadPoolExecutor hard-coded (4 workers max)                         │
│  - No tuning strategy (CPU-bound vs I/O-bound)                           │
│  - Database connection pool size not configurable                        │
│  - Concurrent scans may share threadlocal state issues                   │
│  - No request caching (repeated targets)                                 │
│  - Async/await inconsistent (some sync blocks in async code)             │
│                                                                           │
│ Where: nexura/runner.py, nexura/ai_engine.py, nexura/history_db.py      │
│ Impact: Throughput, latency, scalability                                │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Configurable worker pools (os.cpu_count())                           │
│  2. Async refactoring (full async/await)                                 │
│  3. Result caching (Redis or in-memory with TTL)                         │
│  4. Query optimization (N+1 queries in history_db)                       │
│  5. Connection pooling (asyncpg for future DB)                           │
│  6. Profiling & benchmarking                                             │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B4: SECURITY & INPUT VALIDATION ──────────────────────────────────────┐
│ Problem:                                                                  │
│  - ✅ FIXED: UI basic terminal endpoint safeguards                       │
│  - ❌ TBD: SQL injection risk DB (parametrized queries OK lekin)          │
│  - ❌ TBD: Command injection risk subprocess calls                       │
│  - ❌ TBD: Path traversal'dan o'tkazmadi (cd command'da)                  │
│  - ❌ TBD: XXE attack'dan vulnerable XML parsing                         │
│  - ❌ TBD: SSRF risk (whatweb arbitrary URL)                              │
│  - ❌ TBD: Authentication/authorization yo'q (multi-user)                │
│                                                                           │
│ Where: nexura/web/app.py, nexura/parsers/nmap.py, nexura/scanners/network.py
│ Impact: Critical vulnerabilities, data breach                           │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Input whitelisting (targets — IP/hostname/URL only)                 │
│  2. Command escaping (shell injection prevention)                        │
│  3. XXE prevention (defusedxml)                                          │
│  4. SSRF prevention (target validation against private ranges)           │
│  5. Authentication layer (JWT tokens)                                    │
│  6. Role-based access control (RBAC)                                     │
│  7. Rate limiting + API key validation                                   │
│  8. Security headers (CSP, X-Frame-Options, etc.)                        │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B5: DATA VALIDATION & PARSING ROBUSTNESS ─────────────────────────────┐
│ Problem:                                                                  │
│  - nmap.py line 31: .find("ports") None bo'lsa crash                    │
│  - nuclei.py: line finding regex fragile (format change'da fail)         │
│  - nikto.py, sqlmap.py, gobuster.py similar fragility                   │
│  - whatweb.py JSON parsing incomplete error handling                     │
│  - amass parsing regex-dependent                                        │
│                                                                           │
│ Where: nexura/parsers/*.py                                              │
│ Impact: Scanner reliability, missing data                               │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Defensive XML parsing (with defaults)                                │
│  2. Parser unit tests (comprehensive fixtures)                           │
│  3. Regex pattern versioning (tool version checks)                       │
│  4. Fallback parser chain (format1 -> format2 -> plaintext)              │
│  5. Parser extensibility (plugin architecture)                           │
│  6. Output validation against schema                                     │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B6: TESTING & CODE QUALITY ───────────────────────────────────────────┐
│ Problem:                                                                  │
│  - ✅ Basic unit tests (45 tests)                                        │
│  - ❌ TBD: Integration tests incomplete (no real tool integration)       │
│  - ❌ TBD: E2E tests yo'q (browser automation)                            │
│  - ❌ TBD: Load testing yo'q                                             │
│  - ❌ TBD: Security testing (OWASP, fuzzing) yo'q                        │
│  - ❌ TBD: Mock external APIs incomplete                                 │
│  - ❌ TBD: Code coverage metrics yo'q                                    │
│                                                                           │
│ Where: tests/ folder                                                    │
│ Impact: Regression risk, feature confidence                             │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Pytest coverage (pytest-cov, target 80%+)                            │
│  2. Mock external tools (fake nmap output)                               │
│  3. Integration tests (docker-based tool instances)                      │
│  4. Load testing (locust)                                                │
│  5. Security testing (bandit, safety)                                    │
│  6. Mutation testing (mutmut)                                            │
│  7. Type checking (mypy or pyright)                                      │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B7: API DESIGN & VERSIONING ──────────────────────────────────────────┐
│ Problem:                                                                  │
│  - No API versioning (/api/v1, /api/v2)                                 │
│  - Response format inconsistent (sometimes nested objects, arrays)      │
│  - No pagination for history (/api/history returns all)                 │
│  - No filtering/sorting endpoints                                       │
│  - No deprecation strategy                                              │
│  - Error codes not standard HTTP statuses use                           │
│                                                                           │
│ Where: nexura/web/app.py                                                │
│ Impact: Client compatibility, maintainability                           │
│                                                                           │
│ SOLUTION:                                                                │
│  1. API versioning (middleware-based)                                    │
│  2. Standardized response envelope                                       │
│  3. Pagination (limit/offset)                                            │
│  4. Query filters (e.g., /api/history?status=completed&target=example)  │
│  5. OpenAPI/Swagger documentation                                       │
│  6. Deprecation headers                                                  │
│  7. Proper HTTP status codes                                             │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B8: DOCUMENTATION & DEVELOPER EXPERIENCE ─────────────────────────────┐
│ Problem:                                                                  │
│  - README.md mixed English/Uzbek, outdated                              │
│  - API documentation yo'q (inline docstrings incomplete)                 │
│  - Setup guide minimal, troubleshooting section yo'q                     │
│  - Code comments sparse (business logic unclear)                         │
│  - Architecture diagram / design docs yo'q                               │
│  - Contributing guide yo'q                                              │
│                                                                           │
│ Where: README.md, Code files                                            │
│ Impact: Onboarding time, maintenance difficulty                         │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Comprehensive README (setup, usage, architecture)                    │
│  2. API documentation (Swagger/OpenAPI)                                  │
│  3. Developer guide (setup dev environment, debugging)                   │
│  4. Architecture documentation                                           │
│  5. Contributing guidelines (code style, PR process)                     │
│  6. Troubleshooting guide                                                │
│  7. Docstrings (Google style)                                            │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B9: INCONSISTENT INTERNATIONALIZATION (i18n) ─────────────────────────┐
│ Problem:                                                                  │
│  - Error messages inglizcha/uzbekcha aralash                             │
│  - User-facing strings hardcoded (no i18n framework)                     │
│  - Frontend (React) yo'q bo'lsa i18n kerak yo'q, lekin backend'da mixed │
│  - Logging messages inconsistent language                                │
│                                                                           │
│ Where: Throughout codebase                                              │
│ Impact: User confusion, maintainability                                 │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Decide: Uzbek-only yoki English-only (consistent)                    │
│  2. String constants configuration-based                                 │
│  3. Logging formatter language-aware                                     │
│  4. Frontend i18n library (if multilingual needed)                       │
└────────────────────────────────────────────────────────────────────────────┘

┌─ B10: DEPENDENCIES & LIBRARY MANAGEMENT ────────────────────────────────┐
│ Problem:                                                                  │
│  - ✅ pyproject.toml modern (Python 3.10+)                               │
│  - ❌ TBD: No pinned versions (security risk)                            │
│  - ❌ TBD: Compatibility matrix documentation yo'q                       │
│  - ❌ TBD: Security vulnerability scanning missing                       │
│  - ❌ TBD: Dependency conflict detection yo'q                            │
│  - ❌ TBD: License compliance check yo'q                                 │
│                                                                           │
│ Where: pyproject.toml                                                   │
│ Impact: Supply chain security, stability                                │
│                                                                           │
│ SOLUTION:                                                                │
│  1. Pin dependency versions (requirements.txt)                           │
│  2. Security scanning (safety, dependabot)                               │
│  3. Dependency conflicts check (pip-audit)                               │
│  4. License scanning (FOSSA, Black Duck)                                 │
│  5. Update strategy (semantic versioning respect)                        │
│  6. Compatibility matrix documentation                                   │
└────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

📊 SUMMARY STATISTICS
═══════════════════════════════════════════════════════════════════════════

A QISMI (SERVER/DEPLOYMENT):           10 Major Categories
  - Critical Issues:                    2 (Security, tools dependency)
  - High Issues:                        4 (Model, DB, Logging, Rate limiting)
  - Medium Issues:                      4 (Certificate, System service, etc)

B QISMI (CODE/TECHNOLOGY):              10 Major Categories
  - Critical Issues:                    0 (Fixed critical bugs)
  - High Issues:                        2 (Security validation, performance)
  - Medium Issues:                      8 (Parsers, testing, docs, etc)

Total Major Improvement Areas:          20
Estimated Implementation Effort:        4-8 weeks

═══════════════════════════════════════════════════════════════════════════

🎯 PRIORITET ROADMAP
═══════════════════════════════════════════════════════════════════════════

SPRINT 1 (Week 1-2) — CRITICAL ISSUES:
  ✓ A1: External tools setup documentation
  ✓ A4: Environment variables & secrets management
  ✓ B4: Security validation (input whitelisting)

SPRINT 2 (Week 3-4) — HIGH PRIORITY:
  ✓ A6: HTTPS/SSL setup (nginx + Let's Encrypt)
  ✓ A3: Database migration system
  ✓ B1: Pydantic validators

SPRINT 3 (Week 5-6) — MEDIUM PRIORITY:
  ✓ A9: Rate limiting
  ✓ A5: Structured logging
  ✓ B2: Error handling improvements

SPRINT 4 (Week 7-8) — NICE-TO-HAVE:
  ✓ A7: Systemd service
  ✓ B6: Testing & code quality
  ✓ B8: Documentation

═══════════════════════════════════════════════════════════════════════════

🚀 NEXT STEPS
═══════════════════════════════════════════════════════════════════════════

1. A QISMI (SERVER): Deployment checklist yaratish
2. B QISMI (CODE): Bug tracking system'da issue register
3. Risk assessment va prioritization
4. Resource allocation (team size, timeline)
5. Automated testing pipeline setup
6. CI/CD configuration

═══════════════════════════════════════════════════════════════════════════

